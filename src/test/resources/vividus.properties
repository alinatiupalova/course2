#webdriver.type=chrome,firefox
#webdriver.driver-options.chrome.arguments=--headless,--disable-gpu
#webdriver.driver-options.firefox.arguments=--headless
#tests.batch.size=1


# execute scripts on your local machine in Chrome browser in desktop mode and default resolution:

configuration.profiles=web/desktop/chrome,firefox

# pick up suite related properties from 'demo' suite folder:

configuration.suites=stories

# pick up environment related properties from 'saucedemo' environment folder:

configuration.environments=environment.properties